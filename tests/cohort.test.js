import { test } from 'vitest';

test.todo('GET /api/cohort returns all dishes');
test.todo('GET /api/cohort/:id returns a dish');
test.todo('GET /api/cohort/:id returns 404 for unknown id');
test.todo('GET /api/cohort/area/:area filters by area');
test.todo('GET /api/cohort/added_by/:added_by filters by author');
